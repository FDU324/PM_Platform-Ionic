export class Moment {
    constructor(
        public id: string,
        public username: string,
        public gameId: string,
        public time: number,
        public title: string,
        public image: string, ) {
    }
}